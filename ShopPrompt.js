function OpenDialog(){
    var dialog = document.getElementById('dialog');
    dialog.showModal();

}
function CloseDialog(){
    var dialog = document.getElementById('dialog');
    dialog.close();

}